


function [qhd] = tvh_style_manip(qhd,vibr_amp,vibr_freq)

% scale the maginitude of harmonics
taxis = (0:size(qhd.v_am,1)-1)*1/qhd.Fs;
qhd.new_v_fm = qhd.v_fm;
for i = 1:qhd.nharm
    qhd.new_v_fm(:,i) = qhd.v_fm(:,i)+i*vibr_amp*sin(2*pi*vibr_freq*taxis(:));
end
end
